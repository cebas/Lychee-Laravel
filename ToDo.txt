para instalar
	git clone --depth 1 --recurse-submodules https://github.com/cebas/Lychee-Laravel.git lychee
	crear .env
	Generate your secret key
		php artisan key:generate
	Migrate your database to create a new database or migrate an existing Lychee installation to the new framework.
		php artisan migrate

	copiar settings y user table desde extinto

	habilitar modulo headers en apache
	habilitar headers en .htaccess para poder acceder desde otro dominio
	ver que esten instalados los modulos de php para imagenes (GD, exif, etc)

cambios
	sacar del UI el boton de settings
